@model IEnumerable<LsMapasNet.Entidade.MapaSurdo>


<ul class="collection with-header">
    <li class="collection-header pink darken-3 "><h4 class="white-text">First Names</h4></li>
    @foreach (var item in Model)
    {
        <li class="collection-item"><div>@item.id_surdo.nome&nbsp;<a href="@Url.Action(" Delete", "DeleteSurdoMapa" ,new { id=item.id })" class="secondary-content"><i class="Medium pink-text  text-darken-3 material-icons">delete_forever</i></a></div></li>
    }
</ul>
